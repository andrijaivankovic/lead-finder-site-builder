#!/bin/sh
venv_python="$(dirname "$0")/venv/bin/python"
if [ ! -x "$venv_python" ]; then
    echo "There is no venv yet. Create it first:" >&2
    echo "  python3 -m venv venv" >&2
    echo "  venv/bin/python -m pip install -r requirements.txt" >&2
    exit 1
fi
exec "$venv_python" "$@"
